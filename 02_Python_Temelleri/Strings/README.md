# W02: Metin İşlemleri (Strings)

Bu klasör, metinlerle (String) nasıl çalışılacağını öğretir.

## 📚 İçerik
1.  **String Tanımlama**: "Merhaba Dünya".
2.  **Dilimleme (Slicing)**: Metnin parçalarını alma (`text[0:5]`).
3.  **Metotlar**: `upper()`, `lower()`, `replace()`, `split()`.
4.  **Formatlama**: f-string kullanımı (`f"İsim: {isim}"`).

## 🎯 Hedef
Metinleri manipüle etme ve kullanıcıya düzgün çıktılar verme yeteneği.
