# W11: Modern Web API (FastAPI)

Bu klasör, Python ile hızlı ve modern web servisleri (API) yazmayı öğretir.

## 📚 İçerik
1.  **API Nedir?**: İstemci ve Sunucu iletişimi.
2.  **FastAPI Kurulumu**: `pip install fastapi uvicorn`.
3.  **Endpoint**: URL tanımlama (`@app.get("/")`).
4.  **Swagger UI**: Otomatik dökümantasyon (`/docs`).

## 🎯 Hedef
Verileri web üzerinden erişilebilir hale getirmek (Backend Developer).
